[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "celery -A app.celery_app.celery_app worker -Q light --concurrency=2 --loglevel=info"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10